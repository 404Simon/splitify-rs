# Splitify Environment Variables
# Copy this file to .env and customize for your environment

# =============================================================================
# DOCKER USER MAPPING (for docker-compose)
# =============================================================================

# Set to your host user/group IDs to avoid permission issues with mounted volumes
# Get your IDs with: id -u (UID) and id -g (GID)
# Default: 1000:1000 (common default user on Linux)
UID=1000
GID=1000

# =============================================================================
# DATABASE CONFIGURATION
# =============================================================================

# SQLite database location
# Default: sqlite:splitify.db (relative to project root)
DATABASE_URL=sqlite:splitify.db

# =============================================================================
# RECURRING DEBTS SCHEDULER
# =============================================================================

# Cron expression for automated recurring debt generation
# Format: "second minute hour day_of_month month day_of_week"
# Default: "0 0 6 * * *" (daily at 6:00 AM UTC)
#
# Common patterns:
#   Daily at 6 AM:       0 0 6 * * *
#   Daily at midnight:   0 0 0 * * *
#   Every hour:          0 0 * * * *
#   Every 15 minutes:    0 */15 * * * *
#   Weekdays at 9 AM:    0 0 9 * * 1-5
#   Monthly (1st @ 3AM): 0 0 3 1 * *
#
# See docs/RECURRING_DEBTS_SCHEDULER.md for detailed configuration
RECURRING_DEBTS_CRON="0 0 6 * * *"

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server address and port (configured in Cargo.toml [package.metadata.leptos])
# Default: 127.0.0.1:3000
# LEPTOS_SITE_ADDR=127.0.0.1:3000

# =============================================================================
# SESSION CONFIGURATION
# =============================================================================

# Session cookie SameSite policy
# Options: "strict" (default), "lax", "none"
# - strict: Cookie only sent on same-site requests (most secure, default)
# - lax: Cookie sent on top-level navigation (good for cross-device testing)
# - none: Cookie sent on all requests (requires SESSION_SECURE=true)
# Default: strict
#SESSION_SAME_SITE=strict

# Session cookie Secure flag
# Options: "true" (default), "false"
# - true: Cookie only sent over HTTPS (required for production)
# - false: Cookie sent over HTTP (for local development only)
# Default: true
#SESSION_SECURE=true
